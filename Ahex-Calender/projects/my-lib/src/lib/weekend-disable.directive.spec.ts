import { WeekendDisableDirective } from './weekend-disable.directive';

describe('WeekendDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new WeekendDisableDirective();
    expect(directive).toBeTruthy();
  });
});
